// ==========================================
// TRANSLATION DICTIONARY (BILINGUAL SYSTEM)
// ==========================================
const translations = {
  tr: {
    // Nav links
    nav_home: "ANASAYFA",
    nav_about: "HAKKIMIZDA",
    nav_menu: "MENÜ",
    nav_contact: "İLETİŞİM",
    
    // Home Page About Section
    about_subtitle: "LEZZETİN CESUR HALİNİ KEŞFET!",
    about_desc: "2026 yılında kurulan Dyners, cesur lezzetleri ve premium kalitesiyle fark yaratmayı hedefliyor. Özenle seçilmiş malzemeler ve ustalıkla hazırlanan tariflerle her lokmada benzersiz bir deneyim sunuyoruz. Kaliteden ödün vermeden, lezzetin tadını her ısırıkta hissettirerek yolculuğumuza devam ediyoruz.",
    about_btn: "Detaylı İncele",
    
    // Home Page Products Section
    products_title: "ÜRÜNLER",
    
    // Products details
    prod_smash_desc: "Smash Köfte - Karamelize soğan - Cheddar Peyniri - Trüflü Mayonez. Patates kızartması ile servis edilir.",
    prod_smash_xl_desc: "2 adet Smash Köftesi - Karamelize soğan - Cheddar Peyniri - Trüflü Mayonez. Patates kızartması ile servis edilir.",
    prod_smash_xxl_desc: "3 adet Smash Köftesi - Karamelize soğan - Cheddar Peyniri - Trüflü Mayonez. Patates kızartması ile servis edilir.",
    prod_hamburger_desc: "Hamburger Köftesi - Domates - Marul - Turşu - Ketçap - Mayonez. Patates Kızartması ile servis edilir. (100 gr hamburger köftesi, 90 gr hamburger ekmeği)",
    prod_double_hamburger_desc: "2 adet 100gr hamburger köftesi - Domates - Marul - Turşu - Ketçap - Mayonez. Patates Kızartması ile servis edilir.",
    prod_cheeseburger_desc: "Hamburger Köftesi - Cheddar Peyniri - Domates - Marul - Turşu - Ketçap - Mayonez. Patates Kızartması ile servis edilir.",
    prod_double_cheeseburger_desc: "2 adet 100gr hamburger köftesi - Çift Cheddar Peyniri - Domates - Marul - Turşu - Ketçap - Mayonez. Patates Kızartması ile servis edilir.",
    prod_lokum_desc: "Bonfile Lokum Et - Karamelize Soğan - Cheddar Peyniri - Hardal. Patates Kızartması ile servis edilir. (100 gr ve 150 gr bonfile lokum eti seçeneği)",
    prod_chicken_desc: "Çıtır Tavuk - Marul - Cheddar Peyniri - Mayonez. Patates Kızartması ile servis edilir.",
    prod_sosis_desc: "Özel Hamburger Sosisi - Cheddar Peyniri - Marul - Mayonez. Patates Kızartması ile servis edilir.",
    prod_marsvic_desc: "Jülyen Kesim Hamburger Sosisi - Cheddar Peyniri - Marul - Mayonez. Patates Kızartması ile servis edilir.",

    // Contact Section
    contact_title: "BİZE ULAŞIN",
    contact_address_title: "ADRESİMİZ",
    contact_hours_title: "ÇALIŞMA SAATLERİ",
    contact_phone_title: "İLETİŞİM BİLGİLERİ",
    contact_hours_weekdays: "Pazartesi - Cumartesi: 12:00 - 21:00",
    contact_hours_sunday: "Pazar: Kapalı",
    
    // About Page Content
    about_page_title: "HAKKIMIZDA",
    about_page_subtitle: "DYNERS | TASTE BOLDLY",
    about_milestone1_title: "Lezzet Yolculuğumuz",
    about_milestone1_desc: "%100 dana etinden özel ev yapımı köfteyi en kaliteli malzemelerle buluşturup özenle hazırladığımız soslarımız ve taze sebzelerle bir araya getiriyoruz. Yerel fırınlarımızda hazırlanan yumuşacık ekmeklerin arasında son dokunuşları yapıp size kat kat lezzet sunmaktan mutluluk duyuyoruz.",
    about_milestone2_title: "Kalite Anlayışımız",
    about_milestone2_desc: "Dyners ailesi olarak kaliteden asla ödün vermiyoruz. Kullandığımız etlerden sebzelere, soslardan ekmeklerimize kadar her detayı titizlikle kontrol ediyor, sürdürülebilir lezzeti mutfağımızın merkezine yerleştiriyoruz.",
    about_card_chicken_title: "Çıtır Tavuk Burger",
    about_card_chicken_desc: "Çıtır tavuk, taze marul, cheddar peyniri ve kremamsı mayonez ile birleşen çıtır tavuk burgerimiz, her ısırıkta mükemmel bir lezzet uyumu sunuyor.",
    about_card_lokum_title: "Lokum Burger",
    about_card_lokum_desc: "Dinlendirilmiş dana bonfile ile hazırlanan lokum etlerimizi karamelize soğan, hardal ve yumuşacık ekmeğimizle bir araya getirdik.",
    about_partners_title: "PREMİUM İÇERİK ORTAKLARIMIZ",

    // Menus Page Content
    menu_page_title: "MENÜLERİMİZ",
    menu_page_subtitle: "CESUR LEZZETLERİN ADRESİ DYNERS'A HOŞ GELDİNİZ!",
    menu_page_desc: "Özel ev yapımı köfte, sous vide tekniği ile pişirilmiş tiftik tavuk, daha önce denenmemiş sosis hamburger… 11 farklı hamburger çeşidimiz ile lezzet yolculuğunun tadını çıkarın.",
    
    // Footer & Privacy
    footer_tagline: "Lezzetin Cesur Hali.",
    footer_kvkk: "Kişisel Verilerin Korunması",
    footer_privacy: "Gizlilik ve Çerez Politikası",
    footer_rights: "Dyners. Tüm Hakları Saklıdır.",
    footer_developer: "Designed & Powered By"
  },
  en: {
    // Nav links
    nav_home: "HOME",
    nav_about: "ABOUT US",
    nav_menu: "MENU",
    nav_contact: "CONTACT",
    
    // Home Page About Section
    about_subtitle: "DISCOVER THE BOLD STATE OF TASTE!",
    about_desc: "Established in 2026, Dyners aims to make a difference with its bold flavors and premium quality. We offer a unique experience in every bite with carefully selected ingredients and masterfully crafted recipes. We continue our journey by delivering authentic flavor without compromising on quality.",
    about_btn: "Read More",
    
    // Home Page Products Section
    products_title: "OUR PRODUCTS",
    
    // Products details
    prod_smash_desc: "Smash Patty - Caramelized onions - Cheddar Cheese - Truffle Mayonnaise. Served with french fries.",
    prod_smash_xl_desc: "2 Smash Patties - Caramelized onions - Cheddar Cheese - Truffle Mayonnaise. Served with french fries.",
    prod_smash_xxl_desc: "3 Smash Patties - Caramelized onions - Cheddar Cheese - Truffle Mayonnaise. Served with french fries.",
    prod_hamburger_desc: "Hamburger Patty - Tomato - Lettuce - Pickle - Ketchup - Mayonnaise. Served with french fries. (100g patty, 90g artisanal bun)",
    prod_double_hamburger_desc: "Double 100g Hamburger Patty - Tomato - Lettuce - Pickle - Ketchup - Mayonnaise. Served with french fries.",
    prod_cheeseburger_desc: "Hamburger Patty - Cheddar Cheese - Tomato - Lettuce - Pickle - Ketchup - Mayonnaise. Served with french fries.",
    prod_double_cheeseburger_desc: "Double 100g Hamburger Patty - Double Cheddar Cheese - Tomato - Lettuce - Pickle - Ketchup - Mayonnaise. Served with french fries.",
    prod_lokum_desc: "Beef Tenderloin Steak - Caramelized Onions - Cheddar Cheese - Mustard. Served with french fries. (100g and 150g beef steak options)",
    prod_chicken_desc: "Crispy Chicken - Lettuce - Cheddar Cheese - Mayonnaise. Served with french fries.",
    prod_sosis_desc: "Special Hamburger Sausage - Cheddar Cheese - Lettuce - Mayonnaise. Served with french fries.",
    prod_marsvic_desc: "Julienne Cut Hamburger Sausage - Cheddar Cheese - Lettuce - Mayonnaise. Served with french fries.",

    // Contact Section
    contact_title: "CONTACT US",
    contact_address_title: "ADDRESS",
    contact_hours_title: "WORKING HOURS",
    contact_phone_title: "CONTACT INFO",
    contact_hours_weekdays: "Monday - Saturday: 12:00 - 21:00",
    contact_hours_sunday: "Sunday: Closed",
    
    // About Page Content
    about_page_title: "ABOUT US",
    about_page_subtitle: "DYNERS | TASTE BOLDLY",
    about_milestone1_title: "Our Flavor Journey",
    about_milestone1_desc: "We bring together 100% beef homemade patties with the highest quality ingredients, combining them with carefully crafted house sauces and fresh vegetables. We are happy to serve you layer upon layer of flavor inside soft buns baked fresh in local bakeries.",
    about_milestone2_title: "Our Quality Approach",
    about_milestone2_desc: "As the Dyners family, we never compromise on quality. We inspect every detail, from the meat and vegetables we use to our sauces and buns, placing sustainable taste at the heart of our kitchen.",
    about_card_chicken_title: "Crispy Chicken Burger",
    about_card_chicken_desc: "Combining crispy chicken, fresh lettuce, cheddar cheese, and creamy mayonnaise, our crispy chicken burger offers perfect harmony in every bite.",
    about_card_lokum_title: "Steak Burger",
    about_card_lokum_desc: "We brought together our aged beef tenderloin slices with caramelized onions, mustard, and our soft signature bun.",
    about_partners_title: "OUR PREMIUM INGREDIENT PARTNERS",

    // Menus Page Content
    menu_page_title: "OUR MENUS",
    menu_page_subtitle: "WELCOME TO DYNERS, THE HOME OF BOLD FLAVORS!",
    menu_page_desc: "Artisanal homemade patties, pulled chicken cooked with sous vide technique, unprecedented sausage hamburger... Enjoy the flavor journey with our 11 different burger varieties.",
    
    // Footer & Privacy
    footer_tagline: "The Bold State of Taste.",
    footer_kvkk: "KVKK Protection Policy",
    footer_privacy: "Privacy & Cookie Policy",
    footer_rights: "Dyners. All Rights Reserved.",
    footer_developer: "Designed & Powered By"
  }
};

let currentLang = localStorage.getItem("dyners_lang") || "tr";

// Apply active translations across the document
function applyTranslations() {
  const elements = document.querySelectorAll("[data-translate]");
  elements.forEach(el => {
    const key = el.getAttribute("data-translate");
    if (translations[currentLang] && translations[currentLang][key]) {
      el.textContent = translations[currentLang][key];
    }
  });

  // Update URL active states or switcher label
  const labelEl = document.querySelector(".lang-switcher .current-lang .lang");
  if (labelEl) {
    labelEl.textContent = currentLang === "tr" ? "Türkçe" : "English";
  }
  
  // HTML tag lang update
  document.documentElement.setAttribute("lang", currentLang);
}

// Set active language
function setLanguage(lang) {
  currentLang = lang;
  localStorage.setItem("dyners_lang", lang);
  applyTranslations();
}

// ==========================================
// DOCUMENT READY CONTROLLER
// ==========================================
document.addEventListener("DOMContentLoaded", () => {
  // 1. Initialize Translation System
  applyTranslations();

  // 1b. Hero Ticker Dead-Center Fix — runs FIRST, wrapped in try/catch,
  // so it can never be silently skipped by an error thrown later in
  // this handler.
  //
  // THE REAL BUG (found via an on-page debug readout): each band's own
  // geometric center genuinely was landing exactly on the anchor's
  // center (delta measured 0.0px) — but the visible "X" where the two
  // diagonal bands cross is NOT the same point as either band's own
  // center. .first sits near the TOP of .tickers (top:90px) rotated
  // -7.63deg, .second sits near the BOTTOM (bottom:90px) rotated
  // +7.63deg — two different pivot points, vertically apart. Two lines
  // through two different points at opposite angles cross at a point
  // that's offset from both pivots' shared x by (Δy / (2*tan angle)) —
  // in this layout that offset works out to several hundred px, which
  // is exactly the leftward gap that kept showing up no matter how
  // precisely each band was individually centered.
  //
  // Fix: solve for where the two rotated bands ACTUALLY cross right
  // now (basic line-intersection, using each band's measured pivot
  // point + rotation angle), compare that to the target center, and
  // shift BOTH bands by the SAME delta — shifting two lines by an
  // equal amount shifts their intersection by that same amount, so
  // this is exact regardless of the vertical offset between them.
  try {
    const tickersEl = document.querySelector(".tickers");
    // The black checkered "pill" visible behind the hero photo is NOT
    // the photo itself — it's .home-spine::before, a pseudo-element
    // with a huge negative top offset that bleeds all the way up
    // behind the hero/ticker area. You can't get a JS reference to a
    // ::before, but its own CSS (left:50%; transform:translateX(-50%))
    // always centers it on .home-spine's own box, so measuring
    // .home-spine directly gives the exact same center.
    const anchorEl =
      document.querySelector(".home-spine") ||
      document.getElementById("home-video-caller") ||
      document.querySelector("#first-section .video-group");
    if (tickersEl && anchorEl) {
      const baseRotation = { first: -7.63, second: 7.63 };
      const centerTickerBands = () => {
        const anchorRect = anchorEl.getBoundingClientRect();
        const anchorCenter = anchorRect.left + anchorRect.width / 2;

        const firstEl = tickersEl.querySelector(".ticker-container.first");
        const secondEl = tickersEl.querySelector(".ticker-container.second");
        if (!firstEl || !secondEl) return;

        // Reset both to their plain rotation (no translate) before
        // measuring, so repeated calls don't compound on top of a
        // previous correction.
        firstEl.style.transform = `rotate(${baseRotation.first}deg)`;
        secondEl.style.transform = `rotate(${baseRotation.second}deg)`;

        const r1 = firstEl.getBoundingClientRect();
        const r2 = secondEl.getBoundingClientRect();
        // Rotation is around each element's own center, so the AABB's
        // center from getBoundingClientRect equals the true (x,y)
        // pivot the rotation happens around.
        const x1 = r1.left + r1.width / 2;
        const y1 = r1.top + r1.height / 2;
        const x2 = r2.left + r2.width / 2;
        const y2 = r2.top + r2.height / 2;

        const m1 = Math.tan((baseRotation.first * Math.PI) / 180);
        const m2 = Math.tan((baseRotation.second * Math.PI) / 180);

        // Intersection of line1: y = y1 + m1*(x-x1) and
        // line2: y = y2 + m2*(x-x2)
        let crossX;
        if (Math.abs(m1 - m2) < 1e-6) {
          // Parallel (shouldn't happen with opposite angles) — fall
          // back to the simple average.
          crossX = (x1 + x2) / 2;
        } else {
          crossX = (m1 * x1 - m2 * x2 + (y2 - y1)) / (m1 - m2);
        }

        const delta = anchorCenter - crossX;

        firstEl.style.transform = `translateX(${delta}px) rotate(${baseRotation.first}deg)`;
        secondEl.style.transform = `translateX(${delta}px) rotate(${baseRotation.second}deg)`;
      };
      centerTickerBands();
      window.addEventListener("resize", centerTickerBands);
      window.addEventListener("load", centerTickerBands);
      setTimeout(centerTickerBands, 300);
      setTimeout(centerTickerBands, 1000);
    }
  } catch (err) {
    console.error("Ticker centering failed:", err);
  }

  // 2. Language Switcher Toggles
  const switcher = document.querySelector(".lang-switcher");
  if (switcher) {
    switcher.addEventListener("click", (e) => {
      e.stopPropagation();
      switcher.classList.toggle("open");
    });

    const options = switcher.querySelectorAll(".other-langs li");
    options.forEach(opt => {
      opt.addEventListener("click", () => {
        const val = opt.getAttribute("value");
        setLanguage(val);
      });
    });

    // Close switcher dropdown on click outside
    document.addEventListener("click", () => {
      switcher.classList.remove("open");
    });
  }

  // 3. Mobile Hamburger Menu Toggle
  const mobileBtn = document.querySelector(".mobile-btn");
  const mobileMenu = document.querySelector(".mobile-menu");
  const mobileLayer = document.querySelector(".mobile-menu-layer");
  const mobileClose = document.querySelector(".mobile-menu .close-btn");

  if (mobileBtn && mobileMenu) {
    const toggleMenu = () => {
      mobileBtn.classList.toggle("open");
      mobileMenu.classList.toggle("open");
    };

    mobileBtn.addEventListener("click", toggleMenu);
    if (mobileLayer) mobileLayer.addEventListener("click", toggleMenu);
    if (mobileClose) mobileClose.addEventListener("click", toggleMenu);

    // Close mobile menu when a nav link is clicked
    const links = mobileMenu.querySelectorAll("a");
    links.forEach(l => {
      l.addEventListener("click", () => {
        mobileBtn.classList.remove("open");
        mobileMenu.classList.remove("open");
      });
    });
  }

  // 4. Product Swiper Carousel (Home Page)
  const navImages = document.querySelectorAll(".nav-image");
  const imageGroups = document.querySelectorAll(".product-image-group");
  const infoGroups = document.querySelectorAll(".product-info-group");
  const productTab = document.querySelector(".tab");
  const prodPrev = document.getElementById("prod-prev");
  const prodNext = document.getElementById("prod-next");

  if (navImages.length > 0) {
    const totalProducts = navImages.length;

    // Slide the orange ring (.tab) so it sits behind the active thumbnail
    const positionTab = (activeThumb) => {
      if (!productTab || !activeThumb) return;
      const ringPadding = (activeThumb.offsetWidth - productTab.offsetWidth) / 2;
      productTab.style.left = `${activeThumb.offsetLeft + ringPadding}px`;
    };

    const selectProduct = (index) => {
      const targetIndex = String(index);
      let activeThumb = null;

      navImages.forEach(thumb => {
        const match = thumb.getAttribute("data-index") === targetIndex;
        thumb.classList.toggle("active", match);
        if (match) activeThumb = thumb;
      });
      imageGroups.forEach(g => g.classList.toggle("active", g.getAttribute("data-index") === targetIndex));
      infoGroups.forEach(g => g.classList.toggle("active", g.getAttribute("data-index") === targetIndex));

      if (activeThumb) {
        activeThumb.scrollIntoView({ behavior: "smooth", inline: "center", block: "nearest" });
        positionTab(activeThumb);
      }
    };

    navImages.forEach(thumb => {
      thumb.addEventListener("click", () => selectProduct(thumb.getAttribute("data-index")));
    });

    const getCurrentIndex = () => {
      const active = document.querySelector(".nav-image.active");
      return active ? parseInt(active.getAttribute("data-index"), 10) : 1;
    };

    if (prodPrev) {
      prodPrev.addEventListener("click", () => {
        const current = getCurrentIndex();
        selectProduct(current === 1 ? totalProducts : current - 1);
      });
    }
    if (prodNext) {
      prodNext.addEventListener("click", () => {
        const current = getCurrentIndex();
        selectProduct(current === totalProducts ? 1 : current + 1);
      });
    }

    // Layout (thumbnail offsetLeft) isn't reliable until images/fonts have
    // settled, so position the ring on load and once more shortly after.
    const initTab = () => positionTab(document.querySelector(".nav-image.active"));
    window.addEventListener("load", initTab);
    setTimeout(initTab, 100);
  }

  // 5. Scroll Parallax Assembly Engine (Home Page)
  const parallaxSection = document.getElementById("parallax-section");
  if (parallaxSection) {
    const pImages = parallaxSection.querySelectorAll(".p-image");
    
    const assembleBurger = () => {
      const rect = parallaxSection.getBoundingClientRect();
      const viewHeight = window.innerHeight;
      
      // Calculate how close the center of the parallax section is to the center of the viewport
      const viewportCenter = viewHeight / 2;
      const sectionCenter = rect.top + rect.height / 2;
      
      
      // Distance from screen center
      const distance = sectionCenter - viewportCenter;

      // Only animate if the section is currently visible in the viewport
      if (rect.top < viewHeight && rect.bottom > 0) {
        pImages.forEach(img => {
          const datatype = parseFloat(img.getAttribute("datatype"));
          if (!isNaN(datatype)) {
            // Formula to separate pieces on scrolling up/down
            // The multiply coefficient determines separation sensitivity —
            // lowered further (0.1 -> 0.045) so pieces drift apart only
            // subtly while scrolling instead of sliding a large distance.
            const offset = distance * datatype * 0.01;
            // Horizontal jitter only (matches dyners.com — each piece
            // slides left/right, not up/down), composed with the
            // existing -50% centering via calc().
            img.style.transform = `translateX(calc(-50% + ${offset}px))`;
          }
        });
      }
    };

    // Run once on load and bind scroll
    assembleBurger();
    window.addEventListener("scroll", assembleBurger);
  }
});
